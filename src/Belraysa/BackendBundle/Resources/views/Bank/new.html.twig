<form action="{{ path('bank_create') }}" id="newBank" method="post" {{ form_enctype(form_bank) }}
      class="form-horizontal">
    <div class="form-group">
        <label class="col-sm-2 control-label"><span style="color: red"
                                                    class="required">*&nbsp;</span>Nombre</label>

        <div class="col-sm-10">
            {{ form_widget(form_bank.name, {'attr' : {'class' : 'form-control','placeholder':'Nombre' }}) }}
            {{ form_errors(form_bank.name) }}
        </div>
    </div>
    <div class="form-group">
        <label class="col-sm-2 control-label"><span style="color: red"
                                                    class="required">*&nbsp;</span>Saldo</label>

        <div class="col-sm-10">
            {{ form_widget(form_bank.balance, {'attr' : {'class' : 'form-control','placeholder':'Saldo' }}) }}
            {{ form_errors(form_bank.balance) }}
        </div>
    </div>
    <div class="form-group">
        <label class="col-sm-2 control-label"><span style="color: red"
                                                    class="required">*&nbsp;</span>Espacios</label>

        <div class="col-sm-10">
            {{ form_widget(form_bank.workspaces, {'attr' : {'class' : 'form-control select2me', 'style': 'width:100%' }}) }}
            {{ form_errors(form_bank.workspaces) }}
        </div>
    </div>
    <div class="form-group">
        <label class="col-sm-2 control-label">M&eacute;todos de pago</label>

        <div class="col-sm-10">
            {{ form_widget(form_bank.metodosPago, {'attr' : {'class' : 'form-control select2me', 'style': 'width:100%' }}) }}
            {{ form_errors(form_bank.metodosPago) }}
        </div>
    </div>
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-4 simple-checkbox">
            {{ form_widget(form_bank.enabled, {'attr':{'class':'minimal' }}) }}
            {{ form_errors(form_bank.enabled) }}
            <label for="item-enabled">Activo</label>
        </div>
    </div>
    {{ form_rest(form_bank) }}
    <!-- /.box-footer -->
</form>
