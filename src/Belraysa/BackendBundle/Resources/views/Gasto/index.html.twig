{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gasto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Detalles</th>
                <th>Paguea</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gasto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.detalles }}</td>
                <td>{{ entity.pagueA }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gasto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gasto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gasto_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
