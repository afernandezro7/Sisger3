{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ENA</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ena') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ena_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
