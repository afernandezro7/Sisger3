{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Costo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Paguea</th>
                <th>Detalles</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('costo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.pagueA }}</td>
                <td>{{ entity.detalles }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('costo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('costo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('costo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
